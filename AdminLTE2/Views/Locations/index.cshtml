@model IEnumerable<AdminLTE2.Models.Locations>

@{
    ViewData["Title"] = "Locations";
}
<br />
<div class="content-wrapper">
    <div class="container">
        <h1 class="text-center">Locations</h1>
        <div class="row">
            <div class="col-md-12">
                <p>
                    <a asp-action="Create" class="btn btn-outline-primary">Create New</a>
                </p>
                <div class="table-responsive">
                    <table class="table table-bordered">
                        <thead>
                            <tr>
                                <th>
                                    Adress
                                </th>
                                <th>
                                    Postal code
                                </th>
                                <th>
                                    City
                                </th>
                                <th>
                                    State
                                </th>
                                <th>
                                    Country
                                </th>
                                <th></th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.address)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.postal_code)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.city)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.state)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.Countries.country_name)
                                    </td>
                                    <td>
                                        <a asp-action="Edit" asp-route-id="@item.location_id" class="btn btn-sm btn-outline-warning">Edit</a>
                                        <a asp-action="Details" asp-route-id="@item.location_id" class="btn btn-sm btn-outline-success">Details</a>
                                        <a asp-action="Delete" asp-route-id="@item.location_id" class="btn btn-sm btn-outline-danger">Delete</a>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>
